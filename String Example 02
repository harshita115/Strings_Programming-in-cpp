A program in C++ to find the length of a string without using library function.
Enter a string : String
Length of the string is : 6

***********************************************************************************

#include<iostream>
#include<string.h>
int main()
{
	int len,i;
	char a[100];
	std::cout<<"Enter a string : ";
	gets(a);
	for(i=0; a[i] != '\0' ;i++)
	{	}
	std::cout<<"Length of the string is : "<<i;
	return 0;
}

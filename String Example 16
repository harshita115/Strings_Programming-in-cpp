A program in C++ to Concatenate Two Strings Manually.
First String : This is 
Second String : a string.
This is a string.

**********************************************************************************

#include<iostream>
#include<string.h>
int main()
{
	char a[100], b[100],c[200], ch;
	int i,j;
	std::cout<<"First String : ";
	gets(a);
	std::cout<<"Second String : ";
	gets(b);
	for(i=0;a[i]!=0;i++)
	{
		c[i]=a[i];
	}
	for(j=0;b[j]!=0;j++)
	{
		c[i+j]=b[j];
	}
	c[i+j]='\0';
	std::cout<<c;
	return 0;
}

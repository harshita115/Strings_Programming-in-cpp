#include <bits/stdc++.h>

using namespace std;

int main()
{
    char a[100000], word;
    int count = 1;
    cin>>a;
    for(int i=0; a[i]!='\0'; i++)
    {
        if(a[i]>=65 && a[i]<=90)
        {
            count++;
        }
    }
    cout<<count;
    return 0;
}

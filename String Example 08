A program in C++ to copy one string to another string.
Enter a string : Copy String
The copied String is : Copy String
The length of copied string is : 11

**************************************************************************************

#include<iostream>
#include<string.h>
int main()
{
	char a[100], b[100];
	int i,j,count=0;
	std::cout<<"Enter a string : ";
	gets(a);
	for(i=0,j=0; a[i]!='\0'; i++,j++)
	{
		b[j]=a[i];
		count++;
	}
	std::cout<<"The copied String is : "<<b<<std::endl;
	std::cout<<"The length of copied string is : "<<count;
}
